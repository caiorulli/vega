name: Clojure CI

on:
  push:
    branches: [ deck ]
  pull_request:
    branches: [ deck ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: lein deps
    - name: Run tests
      run: lein test
