name: Clojure CI

on:
  push:
    branches: [ deck ]
  pull_request:
    branches: [ deck ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: ./bin/kaocha
